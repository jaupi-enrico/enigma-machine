package com.enigma.enigmamachine;

import javafx.fxml.FXML;
import javafx.scene.control.Label;

public class EnigmaController {
    // vettore di bottoni
    // uso di GridPane
    // setHgap, setVgap
    // button = new Button(lettera)
    // setPregWidth(800.0/ 10)
    // final char finalLettera = lettera
    // setOnAction(e -> {codice che usa finalLettera, e.getSource()})
    // gridButton.add(buttons[i * 9 + j], j, i, 1, 1)
    // lettera++
    // if lettera == '[' return

    // tastiera reale
    // aggancio pressione tastiera, lo associo al bottone

    //onKeyPressed(KeyEveny keyEvent) {
    // if .getcode.isletterKey
    //  int pos = keyeveny.getcode.getchat.chatat(0) - 'A'
    // buttons[pos].fire
    // buttons[pos]-requestFocus
}